## [File Explorer](https://www.greatfrontend.com/interviews/study/gfe75/questions/user-interface/file-explorer)

### Approach
1. use `recursion` to update
2. use `deep clone` , don't `mutate` array directly
3. use `localeCompare` for sort of string