## [68. if II](https://bigfrontend.dev/quiz/if-II)

### Approach
1. "dev"  => 'if' convert statement to Boolean, Boolean of function is true. 
2. Error  => in 'if' condition foo() was function expression not function declaration. so foo is not hoisted, so foo is not defined. calling the function which is not defined throws error