## [63. in](https://bigfrontend.dev/quiz/in-coercion)

### Approach
1. The property could be a string or symbol representing a property name or array index (non-symbols will be coerced to strings). So, ['foo'] gets coerced to "foo" which is present in obj