## [100. `var` vs `let`](https://bigfrontend.dev/quiz/var-vs-let)

### Approach
1. The scope of a variable declared with var for variables declared outside any function will be global i.e. window.
2. let however, does not create properties of the window object when declared globally (in the top-most scope) 