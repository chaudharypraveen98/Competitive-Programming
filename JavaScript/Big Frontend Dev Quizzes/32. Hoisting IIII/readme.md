## [32. Hoisting IIII](https://bigfrontend.dev/quiz/Hoisting-IIII)

### Approach
1.  imagine all the function declarations moved to the top prior to the execution of code. So, all the initializations of a, b, and c to 1 happen after they are declared(or functions are declared). Hence, typeof returns "number"