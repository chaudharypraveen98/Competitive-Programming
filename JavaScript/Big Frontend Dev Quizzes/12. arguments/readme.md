## [12. arguments](https://bigfrontend.dev/quiz/arguments)

### Approach
1. In this example, the function log expects 4 arguments but we are passing only 3 arguments. Hence, arguments inside this function is an array where only 3 elements are present and the last one is assumed to be undefined and you cannot update it as it was not explicitly passed while invoking the function.