## [40. RegExp.prototype.test](https://bigfrontend.dev/quiz/RegExp-prototype-test)

### Appraoch
1. The test() method expects a string as input, against which to match the regular expression.
2. So, if the input is not a string it simply converts the input to a string and then matches with regex.
3. `^4\d\d$` 👉🏻 Starts with 4 followed by exactly one digit and ending with another digit