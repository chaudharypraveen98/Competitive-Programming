## [66. comma](https://bigfrontend.dev/quiz/comma)

### Approach
1. The comma operator (,) evaluates each of its operands (from left to right) and returns the value of the last operand.
2. This means, func is actually assigned the function bar as it is the last operand; and when invoked it prints this.b i.e. "dev"